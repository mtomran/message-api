machine:
  environment:
    mongodb: 127.0.0.1
    redis: 127.0.0.1
  node:
    version: 0.12.7
      
    
dependencies:
  override:
    - cd node; npm install
test:
  override:
    - cd node; npm test