machine:
  services:
    - docker

dependencies:
  override:
    - cd docker && docker-compose build && docker-compose up -d --no-deps node

test:
  override:
    - docker exec -i docker_node_1 npm test