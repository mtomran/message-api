machine:
  hosts:
    mongodb: 127.0.0.1
    redis: 127.0.0.1
  node:
    version: 0.12.7
      
dependencies:
  override:
    - cd node; npm install
test:
  pre: 
    - cd node; node app.js
  override:
    - cd node; npm test