machine:
    node:
      version: 0.12.7
      environment:
        mongodb: 127.0.0.1
        redis: 127.0.0.1
    
dependencies:
  override:
    - cd node; npm install
test:
  override:
    - cd node; npm test