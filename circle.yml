machine:
  services:
    - docker

dependencies:
  override:
    - cd docker && docker-compose build && docker-compose up -d --no-deps node mongo

test:
  override:
    - docker run -i docker_node_1 npm test